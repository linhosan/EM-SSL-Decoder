name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
#    - uses: actions/checkout@v3
#    - name: Build the Docker image
#      run: docker build . --file Dockerfile --tag linhosan/em-ssl-decoder:$(date +%s)
    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: linhosan
        password: dckr_pat_B8rEWnd8kGSqYlFouwPy0wSCYT4
    - name: Build and push
      uses: docker/build-push-action@v4
      with:
        push: true
        platforms: linux/amd64,linux/arm64
        file: Dockerfile
        tags: linhosan/em-ssl-decoder:latest linhosan/em-ssl-decoder:$(date +%s) 
